[defaults]
inventory = inventory.yml
host_key_checking = False
# Collection installate qui (via requirements.yml), non nel path globale
# ~/.ansible/collections: tiene la dipendenza legata al progetto invece che
# all'utente root della macchina che esegue ansible-playbook.
collections_path = ./collections

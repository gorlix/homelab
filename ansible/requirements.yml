---
# Installare con: ansible-galaxy collection install -r requirements.yml
# (fatto automaticamente da run.sh e dal provisioner Tofu prima di ogni ansible-playbook)
collections:
  # Versione fissata alla prima installazione riuscita (10/08/2026, pve-management).
  - name: onepassword.connect
    version: "2.4.0"
